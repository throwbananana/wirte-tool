# Archive Notes

This directory keeps historical review material, one-off patch files, and migration artifacts that are useful for traceability but are no longer part of the active repository surface.

Keep new product-facing documentation in the repository root or the structured `docs/` subdirectories instead of adding more files back to the root.
